{
  "name": "tg-work-search-bot",
  "version": "0.0.0",
  "description": "raid-api",
  "main": "build/app.js",
  "scripts": {
    "dev": "npm run clean & npm run build & node --env-file .env.local build/app",
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "devDependencies": {
    "@types/node": "^22.1.0",
    "@typescript-eslint/eslint-plugin": "^5.33.0",
    "@typescript-eslint/parser": "^5.33.0",
    "eslint": "^8.21.0"
  },
  "eslintConfig": {
    "parser": "@typescript-eslint/parser",
    "parserOptions": {
      "ecmaVersion": "latest",
      "sourceType": "module"
    },
    "plugins": [
      "@typescript-eslint"
    ]
  },
  "dependencies": {
    "@telegraf/session": "^2.0.0-beta.7",
    "mongodb": "^6.8.0",
    "telegraf": "github:telegraf/telegraf",
    "telegram": "^2.23.10",
    "typescript": "^5.5.4"
  }
}
